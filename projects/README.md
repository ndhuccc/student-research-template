# Projects

Place each research project in an ordinary subdirectory. Do not run \`git init\` inside a project directory.

Every project should have its own \`README.md\` explaining its purpose, entry points, dependencies, and how to run its tests or experiments.
